# 功能清单

> Numas (牛马们) — 打工人首选工作模式。
> 更新规则: 功能落地后同步更新功能清单.

---


> 架构设计见 [架构设计.md](架构设计.md)。


## 框架层 · 启动/架构
- [x] dev.js 单命令启动 (Node≥20 校验 + 依赖自检自装 + 双进程 + 浏览器自动打开)
- [x] npx 单命令分发 (`npx -y github:weizuxiao911/numas`)
- [x] 端口单一事实源 (opencode 24096 / webpack 7788)
- [x] 依赖版本判断 (package+lock 内容 hash, 一致则跳过重复 install)
- [x] watchexec 自检自装 (按平台 brew/apt/winget)

## 框架层 · 文件系统
- [x] codeblitz fs ↔ 宿主机打通 (读 SDK / 写 FsPty)
- [x] 双向实时同步 (watchexec --only-emit-events)
- [x] 删除同步 (墓碑拦截 + InMemory 清理)
- [x] 断循环 (自写 hash 对比 + DIR_HASH 目录标记)
- [x] WORKSPACE_ROOT 真实路径化 (postinstall patch)
- [x] explorer 移动大文件不损坏 (宿主机原子 mv 桥接)
- [x] watcher 异常自动重连
- [x] 大文件分块写入 / 中文路径 encodeURI
- [x] 测试用例 (docs/文件系统设计与测试用例.md, 22 条 ✅)

## 框架层 · 终端
- [x] 远程 PTY (opencode 创建/输入/输出)
- [x] shell 类型正确 (按平台偏好 zsh/bash)
- [x] 右键「在终端打开」定位到子目录
- [x] 跨平台 shell-ops 命令构造器

## 框架层 · 运行时会话
- [x] initRuntime (hostCwd + 默认 shell 注入 + runtime-ready)
- [x] hostCwd 兜底场景自动注入 + reload
- [x] 全局 opencode SDK 单例
- [x] workspace 切换

## 扩展 · 欢迎页
- [x] 欢迎组件 + 空工作区自动打开

## 扩展 · 工作区
- [x] 工作目录选择器 (任意目录可切)
- [x] 最近工作目录记忆

## 扩展 · 文件选择器
- [x] 服务器文件树选择 (文件/目录, 限定工作目录)

## 扩展 · Chat
- [x] AI 对话面板 (会话/消息/流式输出/智能体)
- [x] Markdown 渲染 (marked + shiki 双主题)
- [x] chat → opencode → 宿主机 → explorer 同步

## 扩展 · Ask
- [x] 通用 AI 通道 (ask: 独立会话/流式回调/可取消/看门狗超时)
- [x] 供扩展调用 (PDF 标注生成等)

## 扩展 · PDF 阅读器
- [x] PDF 渲染 (pdf.js 分页/懒加载/缩放/目录)
- [x] 标注: rect 圈选即标记 + 选中文本提取
- [x] 工具栏 11 种 AI 辅助学习工具 (文字按钮)
- [x] 交互能力: 动画演示 (可交互 HTML5)
- [x] 交互能力: 代码示例 (同语言 + 环境安装 + 终端)
- [x] 交互能力: AI讲解 / 翻译 / 总结摘要 / 考点分析 (modal)
- [x] 交互能力: 笔记 / 练习 / 导图 / 闪卡 / PPT大纲 (md 文件)
- [x] 交互体验 (生成中可关闭/可继续圈选/完成通知/生成状态按标注隔离)
- [x] sidecar 持久化 + 刷新恢复 + 旧数据兼容
- [x] 测试用例 (docs/标注功能设计与测试用例.md)

## 扩展 · HTML 查看器
- [x] .html 文件组件查看器

## 扩展 · 打开方式/默认编辑器
- [x] 重写「打开方式」(按右键文件算, 过滤 welcome)
- [x] 「配置默认编辑器」(preference 超时降级 localStorage)
- [x] 二次弹窗焦点修复
- [x] VSCode 风格两行 (label + detail)

## 框架补丁
- [x] customEditors.js (webpack alias, webview 生命周期)
- [x] patch-custom-editor.ts (customEditor 运行时 hook)
- [x] patch-codeblitz-constant.js (postinstall: WORKSPACE_ROOT + 原子 move 桥)

## 服务 · Registry
- [x] vsix 扩展分发 (dev 不启, 前端降级)

## 规划 / 待办
- [ ] 标注: 更多交互能力 (TOOLS 注册表扩展)
- [ ] 标注产物管理 (删除/清理生成的 html/md)
- [ ] 聊天 / 标注产物联动
