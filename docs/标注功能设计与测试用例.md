# 标注功能设计与验收标准 (PDF 标注 → AI 动画演示)

## 1. 设计说明

### 1.1 整体设计

- 用户在 PDF 上鼠标拖选区域 (rect), 圈定后 rect 锚定在页面内 (随滚动/缩放跟随, 不漂移)
- 圈选后弹出 AI ask 风格 popover: 提示词输入 + 交互能力动作 (本次实现「生成动画演示」)
- 「生成动画演示」: 将 当前文件名/页码/rect 位置/圈选文本 + 用户提示词, 通过 ask 请求 opencode 生成自包含 HTML5 动画演示, 保存为 `.html` 文件 (PDF 同目录, 文件名自增不覆盖)
- 生成的 html 路径记录到该标注的 interactions (demo 类型), rect 右下角显示「动画演示」按钮, 点击用编辑器打开该 html
- 标注数据持久化到 sidecar (`.{pdfBasename}.annotation`, PDF 同目录), 走 codeblitz IFileServiceClient (与 explorer/编辑器同链路)

### 1.2 设计原则

- 交互能力 = 可扩展列表 (interactions), 每种 type 有注册表 (渲染/动作); 本次先注册 demo (生成动画演示), 后续按需加新类型
- 标注数据走 codeblitz IFileServiceClient (HTTP), 不直连 service 层 FsPty (分层铁律)
- AI 生成走 AskService (独立会话, 流式回调), 不依赖 chat panel
- 圈选文本由 pdf.js textContent 按 rect 相交提取, 作为「内容」上下文传给 AI

### 1.3 核心链路

**单向传播**：
- 圈选 → 锚定 rect + 提取文本 → popover → ask (opencode 生成 HTML) → 保存 .html → sidecar 更新 → rect 按钮
- 点击「动画演示」→ 编辑器打开 html 文件
- 刷新/翻页 → sidecar 重读 → 标注 rect + 按钮恢复

**核心要点**：rect 圈定即锚定 (页面内 absolute), popover 为 AI ask 形态 (非表单), 生成结果落盘与 sidecar 原子一致

## 2. 验收标准

以下操作均在当前工作目录（“/Users/weizuxiao/Documents/鲸海拾贝”）内完成验收, 打开任意 PDF (如 数据结构（C语言版 第2版）.pdf)

### 2.1 rect 圈定与锚定

1. 鼠标在 PDF 页面上拖选区域 (mouseup 后不保存), rect 必须保留显示且锚定在该页内
2. 保持 rect 状态下上下滚动页面, rect 必须跟随页面内容移动, 不得钉在视口 (不"跑")
3. 缩放 (放大/缩小) 后 rect 位置/比例必须正确跟随
4. Esc 或点取消后 rect 移除, sidecar 无残留
5. 误点 (<5px) 不得弹 popover、不得残留 rect

### 2.2 popover AI ask 形态

1. 拖选有文字区域后 popover 弹出, 显示页码 + 圈选文本预览
2. popover 必须为 AI ask 形态 (提示词输入 + 动作按钮), 不得出现旧交互三选控件 (批注/AI讲解/文件)
3. 提示词输入框默认预填: 圈选文本 + "请根据以上内容生成 HTML5 动画演示"

### 2.3 生成动画演示 (interactions demo)

1. 点击「生成动画演示」后按钮必须变 loading, ask 请求发出
2. AI 返回的 HTML 必须保存为 `{pdf名}-动画演示-{n}.html` (PDF 同目录, n 自增不覆盖), 宿主机可见
3. sidecar 中该标注 interactions 必须追加 `{type:'demo', htmlPath, createdAt}`, 旧类型 (comment/prompt) 数据保留
4. 生成后 rect 右下角必须显示「动画演示」按钮
5. 点击「动画演示」按钮必须打开刚生成的 html 文件
6. ask 失败 (超时/网络) 时 popover 内提示错误, 按钮可重试, 不得写入半成品 sidecar
7. 删除标注时已生成的 html 文件必须保留

### 2.4 回归

1. 双击已有标注弹编辑 popover, 保存覆盖不新增条目
2. 翻页/懒加载后各页标注 rect + 按钮位置正确不漂移
3. 刷新页面后标注 + 动画演示按钮恢复
4. 旧 v1 sidecar (含 comment/prompt interactions) 读盘兼容无报错, 数据不丢

## 3. 执行记录

> 执行日期: 2026-09-01; 环境: dev (opencode 24096 + webpack 7788);
> 执行方式: playwright 操作 PDF 阅读器 + 宿主机核对 + ask 链路确认.

| 用例 | 结果 | 备注 |
| --- | --- | --- |
| 2.1-1 | ✅ | 拖选后 rect 锚定到页面内 (absolute), popover 弹出 |
| 2.1-2 | ✅ | 滚动 300px, rect 同步 -300px 跟随, 不钉视口 |
| 2.1-3 | ⏳ | 缩放跟随 (逻辑同滚动, 待重点回归) |
| 2.1-4 | ✅ | 取消后 rect 移除无残留 |
| 2.1-5 | ✅ | <5px 不弹 popover |
| 2.2-1 | ✅ | popover 显示页码 + 圈选文本预览 |
| 2.2-2 | ✅ | 无旧交互三选控件, 提示词 + 生成按钮 |
| 2.2-3 | ✅ | 提示词默认预填 (选中文本 + 生成指令; 无文本时通用指令) |
| 2.3-1 | ✅ | 点击后按钮「生成中…」loading, ask 发出 |
| 2.3-2 | ✅ | 保存 `{pdf名}-动画演示-1.html` 为真 HTML (prompt 强化 + 读回兜底) |
| 2.3-3 | ✅ | sidecar interactions 追加 demo (htmlPath/createdAt) |
| 2.3-4 | ✅ | rect 右下角「动画演示」按钮 (hover 显示) |
| 2.3-5 | ✅ | 点击按钮编辑器打开 html tab |
| 2.3-6 | ✅ | 文件名自增 (目录已有 -1 时生成 -2) |
| 2.3-7 | ⏳ | ask 失败重试 (待断网场景验证) |
| 2.3-8 | ✅ | 删除标注 html 保留 |
| 2.4-1 | ✅ | 双击编辑保存覆盖不新增 |
| 2.4-2 | ⏳ | 翻页懒加载位置 (逻辑同锚定, 待重点回归) |
| 2.4-3 | ✅ | 刷新后标注 + demo 按钮恢复 |
| 2.4-4 | ✅ | 旧 v1 sidecar (prompt) 读盘兼容, 数据保留 |
