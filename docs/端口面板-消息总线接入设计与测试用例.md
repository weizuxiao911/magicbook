# 端口面板设计:消息总线接入 (SSE 收敛)

> 端口面板功能本身 (PTY 进程跟踪 / 反代 / 名称备注) 见 [端口面板-PTY服务跟踪设计与测试用例](./端口面板-PTY服务跟踪设计与测试用例.md).
> 本篇只讲**事件通道改造**: 把端口开/关事件从"自建 SSE"改为消费[消息总线](./消息总线服务设计与测试用例.md)。

## 1. 设计说明

### 1.1 整体设计

**现状**: `service/ports/ports.service.ts` 的 `subscribe()` 内部自建一条 `EventSource('/global/event')` (`ensureSse/closeSse`, ports.service.ts:89-119), 只过滤 `ports.detected` / `ports.closed`, 维护 `cached` 列表并 fan-out 给订阅者. 这与 chat / ask 各自的 SSE 重复 (见[消息总线设计](./消息总线服务设计与测试用例.md))。

**改造**: 端口面板的事件消费改由总线驱动.

- 删除 `ports.service.ts` 里的 `sse` 字段 / `ensureSse()` / `closeSse()` / `new EventSource(...)`.
- `subscribe(cb)` 内部改为调用总线 `onEventType(['ports.detected', 'ports.closed'], handler)`:
  - `handler` 里保留原有缓存更新逻辑 (detected 去重插入 + 按端口排序; closed 移除), 组装 `{ type, port, process }` 后 fan-out 给 `subscribe(cb)` 的监听者。
- 面板 (`extensions/ports/PortsPanel.tsx`) 与其余端口逻辑**完全不变**: 仍是 `portsService.subscribe(cb)` 拿事件、`scan/list/add/remove/proxyUrl/registerPid/unregisterPid` 走原 HTTP 封装。

**关键点**: SSE 连接从 ports.service **上移到消息总线**, ports.service 只保留非 SSE 的业务 (HTTP 增删查 + 缓存). 面板 mount/unmount 的订阅生命周期不变 (unmount 调 unsub → 总线引用计数归 0 时关连接)。

### 1.2 设计原则

- **事件源唯一**: 端口开/关事件只从消息总线取, ports.service 不再持有 EventSource。
- **对外 API 不变**: `IPortsService.subscribe(cb): () => void` 签名与回调载荷 (`{type, port, process?}`) 不变, 面板零改动。
- **缓存语义不变**: detected 去重 + 排序、closed 移除、`cached` 快照逻辑原样保留。
- **非 SSE 逻辑不动**: scan/add/remove/registerPid/proxyUrl 等 HTTP 调用不涉及本次改造。

### 1.3 核心链路

```
[opencode 服务端] PTY 进程扫描 → ports.detected / ports.closed (directory:"global")
      ↓ GlobalBus → /global/event SSE
[消息总线 eventBus] (唯一 SSE, 见总线文档)
      ↓ onEventType(['ports.detected','ports.closed'], handler)
[service/ports/ports.service.ts]
      ├─ 更新 cached 列表 (detected 去重插入排序 / closed 移除)
      └─ fan-out 给 subscribe(cb) 监听者
      ↓
[extensions/ports/PortsPanel.tsx] 列表刷新 + notification (面板挂载时才订阅)
```

**subscribe 改造后形态** (示意):

```ts
subscribe(cb): () => void {
  this.listeners.add(cb);
  const off = onEventType(['ports.detected', 'ports.closed'], (ev) => {
    const type = ev.type as 'ports.detected' | 'ports.closed';
    const port = Number(ev.properties.port);
    if (!port) return;
    const process = ev.properties.process as string | undefined;
    // 更新 cached (沿用原逻辑)
    if (type === 'ports.detected') {
      if (!this.cached.some((e) => e.port === port)) {
        this.cached = [...this.cached, { port, process, detectedAt: Date.now() }]
          .sort((a, b) => a.port - b.port);
      }
    } else {
      this.cached = this.cached.filter((e) => e.port !== port);
    }
    this.listeners.forEach((l) => l({ type, port, process }));
  });
  return () => {
    this.listeners.delete(cb);
    off();
  };
}
```

## 2. 验收标准

### 2.1 事件通道

1. `service/ports/ports.service.ts` 内**不再出现** `new EventSource` / `ensureSse` / `closeSse`。
2. 端口事件经总线到达: 面板开着时, 终端起服务能收到 `ports.detected`, 停掉收到 `ports.closed`。
3. 与对话面板/提问命令同时使用时, Network 中 `/global/event` 仅一条 (共享总线)。

### 2.2 面板功能回归 (沿用 PTY 跟踪文档)

1. 内置终端 `python3 -m http.server 8000` → 面板 ≤5s 出现 `:8000` + 通知; Ctrl+C 后 ≤8s 移除。
2. 手动添加 / ✕ 删除端口、名称备注 (localStorage `ai-ports-labels`)、反代打开 (`/proxy/<port>/`) 全部正常。
3. 面板未挂载时不弹通知 (panel-only 订阅语义不变); mount 时立即 `scan()` 拉一次快照。

### 2.3 订阅生命周期

1. 面板 mount → 订阅; unmount → 取消订阅。
2. 端口面板是唯一订阅者且其卸载后, 若总线引用计数归 0 则 SSE 关闭; 再次 mount 自动重建。
3. 回调载荷 `{type, port, process?}` 与改造前一致, 面板无类型/字段错误。

## 3. 执行记录

> 执行日期: <待填>; 环境: dev (opencode 24096 + webpack 7788);
> 执行方式: 终端起停服务对照面板 + DevTools Network 核对单条 SSE + localStorage/反代回归。

| 用例 | 结果 | 备注 |
| --- | --- | --- |
| 2.1-1 | ⏳ | ports.service 无 new EventSource |
| 2.1-2 | ⏳ | detected/closed 经总线到达 |
| 2.1-3 | ⏳ | 三功能同用 /global/event 仅一条 |
| 2.2-1 | ⏳ | http.server 检测/移除 + 通知 |
| 2.2-2 | ⏳ | 增删/备注/反代正常 |
| 2.2-3 | ⏳ | 未挂载无通知 + mount 拉快照 |
| 2.3-1 | ⏳ | mount 订阅 / unmount 取消 |
| 2.3-2 | ⏳ | 计数归 0 关闭 / 重建 |
| 2.3-3 | ⏳ | 回调载荷字段一致 |
