# 服务地址配置
# 复制为 .env.development 使用；.env.* 不入库，仅本模板提交

# 客户端唯一入口（sandbox: /ai /fs /workspace /sandbox）
APP_BASE_URL=http://127.0.0.1:7789

# 拓展分发服务（registry: vsix 元数据/资源, HTTPS kt-ext 协议, 自签证书需系统信任）
REGISTRY_BASE_URL=https://127.0.0.1:7790
