# 服务地址配置
# 复制为 .env.development 使用；.env.* 不入库，仅本模板提交

# 沙箱调度服务（sandbox: /sandbox + /fs + opencode 生命周期）
SANDBOX_BASE_URL=http://127.0.0.1:7780

# 拓展分发服务（registry: vsix 元数据/上传/下载）
REGISTRY_BASE_URL=http://127.0.0.1:7781