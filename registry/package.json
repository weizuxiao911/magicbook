{
  "name": "registry",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Numas registry — vsix 统一管理与分发: 扫描 vsix/ → 生成 metadata → HTTP 静态分发（kt-ext 兼容 http/https）",
  "main": null,
  "scripts": {
    "dev": "node src/build.js && node src/server.js",
    "build": "node src/build.js",
    "build:config": "tsc src/server.ts src/build.ts --module es2020 --moduleResolution bundler --target ES2020 --esModuleInterop --skipLibCheck --resolveJsonModule --outDir src --rootDir src",
    "serve": "node src/server.js",
    "start": "node src/build.js && node src/server.js",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "adm-zip": "^0.5.12"
  },
  "devDependencies": {
    "@types/adm-zip": "^0.5.5",
    "@types/node": "^22.0.0",
    "typescript": "^5.5.4"
  }
}