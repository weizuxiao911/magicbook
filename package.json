{
  "name": "magicbook",
  "version": "0.1.0",
  "description": "AI 工作台 — codeblitz + opencode 一体化容器 (npx github:weizuxiao911/magicbook)",
  "bin": {
    "cli": "cli/bin/cli.cjs"
  },
  "scripts": {
    "dev": "npm --prefix cli run web -- --port 3100 --hostname 127.0.0.1 --cors http://127.0.0.1:7788",
    "serve": "npm --prefix cli run serve -- --port 3100 --hostname 127.0.0.1"
  },
  "devDependencies": {
    "tsx": "^4.23.12"
  }
}
