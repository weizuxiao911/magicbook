{
  "name": "magicbook",
  "version": "0.1.0",
  "private": true,
  "description": "Magicbook — C/S 架构可交互工作台：sandbox + registry + client",
  "scripts": {
    "dev": "concurrently -k -n sandbox,registry,client -c blue,magenta,green \"npm:dev:sandbox\" \"npm:dev:registry\" \"npm:dev:client\"",
    "dev:sandbox": "npm --prefix server/sandbox run dev",
    "dev:registry": "npm --prefix server/registry run dev",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix server/sandbox run build && npm --prefix server/registry run build && npm --prefix client run build"
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}